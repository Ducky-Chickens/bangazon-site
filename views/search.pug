extends index.pug

block content
  h2 Search results
  //- each product in products
  //-   a(href=`/products/${product.id}`) 
  //-     h3= product.title
  //-   p= product.description
  //-   h4= product.price